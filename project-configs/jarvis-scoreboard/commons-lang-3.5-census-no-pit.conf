# Fast validation overlay: the commons-lang IMPROVED_100 census WITHOUT mutation testing.
# Exercises generation -> build -> JaCoCo -> generalization (minutes), skipping the heavyweight
# PIT stages, to validate the pipeline end-to-end before committing to a full PIT run.
include "commons-lang-3.5-census.conf"

teralizer {
  pitest {
    enabled = false
  }
}
