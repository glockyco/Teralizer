package teralizer.processing;

public enum ProcessingStage {
    CLEANUP_PROJECT(0),
    BUILD_SPOON_MODEL(1),

    DOWNLOAD_PROJECT(2),
    SETUP_PROJECT(3),

    BUILD_PROJECT_ORIGINAL(4),
    GENERATE_TESTS(5),

    EXECUTE_TESTS_ORIGINAL(6),
    COLLECT_JUNIT_REPORTS_ORIGINAL(7),
    ANALYZE_TESTS(8),
    FILTER_TESTS(9),

    ADD_JPF_INSTRUMENTATION(10),
    BUILD_PROJECT_INSTRUMENTED(11),
    EXECUTE_JPF(12),

    ADD_DEPENDENCIES(13),
    BUILD_PROJECT_INITIAL(14),
    EXECUTE_TESTS_INITIAL(15),

    COLLECT_JUNIT_REPORTS_INITIAL(16),
    COLLECT_JACOCO_DATA_INITIAL(17),
    COLLECT_PIT_DATA_INITIAL(18),

    CLEANUP_GENERALIZATION(19),

    GENERALIZE_TESTS(20),
    BUILD_PROJECT_GENERALIZED(21),

    EXECUTE_TESTS_GENERALIZED(22),
    COLLECT_JUNIT_REPORTS_GENERALIZED(23),
    FILTER_GENERALIZATIONS(24),

    COLLECT_JACOCO_DATA_GENERALIZED(25),
    COLLECT_PIT_DATA_GENERALIZED(26);

    private final Integer step;

    ProcessingStage(Integer step) {
        this.step = step;
    }

    public Integer getStep() {
        return this.step;
    }
}
