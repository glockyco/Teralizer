package teralizer.processing;

public enum ProcessingStage {
    CLEANUP_PROJECT(0),

    DOWNLOAD_PROJECT(1),
    SETUP_PROJECT(2),

    ADD_DEPENDENCIES(3),
    BUILD_PROJECT_ORIGINAL(4),

    GENERATE_EVOSUITE_TESTS(5),
    POSTPROCESS_EVOSUITE_TESTS(6),

    BUILD_SPOON_MODEL(7),

    EXECUTE_TESTS_ORIGINAL(8),
    COLLECT_JUNIT_REPORTS_ORIGINAL(9),
    COLLECT_JACOCO_DATA_ORIGINAL(10),
    FILTER_TESTS_ORIGINAL(11),
    COLLECT_PIT_DATA_ORIGINAL(12),

    ANALYZE_TESTS(13),
    FILTER_TESTS(14),
    FILTER_ASSERTIONS(15),

    ADD_JPF_INSTRUMENTATION(16),
    BUILD_PROJECT_INSTRUMENTED(17),
    EXECUTE_JPF(18),
    ANALYZE_JPF(19),
    CLEANUP_JPF_INSTRUMENTATION(20),

    BUILD_PROJECT_INITIAL(21),
    EXECUTE_TESTS_INITIAL(22),

    COLLECT_JUNIT_REPORTS_INITIAL(23),
    COLLECT_JACOCO_DATA_INITIAL(24),
    COLLECT_PIT_DATA_INITIAL(25),

    CLEANUP_GENERALIZATION(26),

    GENERALIZE_TESTS(27),
    BUILD_PROJECT_GENERALIZED(28),

    EXECUTE_TESTS_GENERALIZED(29),
    COLLECT_JUNIT_REPORTS_GENERALIZED(30),
    FILTER_GENERALIZATIONS(31),

    COLLECT_JACOCO_DATA_GENERALIZED(32),
    COLLECT_PIT_DATA_GENERALIZED(33);

    private final Integer step;

    ProcessingStage(Integer step) {
        this.step = step;
    }

    public Integer getStep() {
        return this.step;
    }
}
