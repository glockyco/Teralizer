package teralizer.processing;

public enum ProcessingStage {
    CLEANUP_PROJECT(0),

    DOWNLOAD_PROJECT(1),
    SETUP_PROJECT(2),

    ADD_DEPENDENCIES(3),
    BUILD_PROJECT_ORIGINAL(4),

    GENERATE_EVOSUITE_TESTS(5),
    POSTPROCESS_EVOSUITE_TESTS(6),

    BUILD_SPOON_MODEL(7),

    EXECUTE_TESTS_ORIGINAL(8),
    COLLECT_JUNIT_REPORTS_ORIGINAL(9),
    FILTER_TESTS_ORIGINAL(10),

    ANALYZE_TESTS(11),
    FILTER_TESTS(12),
    FILTER_ASSERTIONS(13),

    ADD_JPF_INSTRUMENTATION(14),
    BUILD_PROJECT_INSTRUMENTED(15),
    EXECUTE_JPF(16),
    ANALYZE_JPF(17),
    CLEANUP_JPF_INSTRUMENTATION(18),

    BUILD_PROJECT_INITIAL(19),
    EXECUTE_TESTS_INITIAL(20),
    COLLECT_JUNIT_REPORTS_INITIAL(21),

    CLEANUP_GENERALIZATION(22),
    GENERALIZE_TESTS(23),
    BUILD_PROJECT_GENERALIZED(24),
    EXECUTE_TESTS_GENERALIZED(25),
    COLLECT_JUNIT_REPORTS_GENERALIZED(26),
    FILTER_GENERALIZATIONS(27),

    RESTORE_ORIGINAL_BUILD(28),
    COLLECT_JACOCO_DATA_ORIGINAL(29),
    COLLECT_PIT_DATA_ORIGINAL(30),
    COLLECT_JACOCO_DATA_INITIAL(31),
    COLLECT_PIT_DATA_INITIAL(32),
    RESTORE_GENERALIZED_BUILD(33),
    COLLECT_JACOCO_DATA_GENERALIZED(34),
    COLLECT_PIT_DATA_GENERALIZED(35);

    private final Integer step;

    ProcessingStage(Integer step) {
        this.step = step;
    }

    public Integer getStep() {
        return this.step;
    }
}
