package teralizer.processing;

public enum ProcessingStage {
    CLEANUP_PROJECT(0),
    BUILD_SPOON_MODEL(1),

    DOWNLOAD_PROJECT(2),
    SETUP_PROJECT(3),

    BUILD_PROJECT_ORIGINAL(4),
    GENERATE_TESTS(5),

    EXECUTE_TESTS_ORIGINAL(6),
    COLLECT_JUNIT_REPORTS_ORIGINAL(7),
    ANALYZE_TESTS(8),
    FILTER_TESTS(9),
    FILTER_ASSERTIONS(10),

    ADD_JPF_INSTRUMENTATION(11),
    BUILD_PROJECT_INSTRUMENTED(12),
    EXECUTE_JPF(13),
    CLEANUP_JPF_INSTRUMENTATION(14),

    ADD_DEPENDENCIES(15),
    BUILD_PROJECT_INITIAL(16),
    EXECUTE_TESTS_INITIAL(17),

    COLLECT_JUNIT_REPORTS_INITIAL(18),
    COLLECT_JACOCO_DATA_INITIAL(19),
    COLLECT_PIT_DATA_INITIAL(20),

    CLEANUP_GENERALIZATION(21),

    GENERALIZE_TESTS(22),
    BUILD_PROJECT_GENERALIZED(23),

    EXECUTE_TESTS_GENERALIZED(24),
    COLLECT_JUNIT_REPORTS_GENERALIZED(25),
    FILTER_GENERALIZATIONS(26),

    COLLECT_JACOCO_DATA_GENERALIZED(27),
    COLLECT_PIT_DATA_GENERALIZED(28);

    private final Integer step;

    ProcessingStage(Integer step) {
        this.step = step;
    }

    public Integer getStep() {
        return this.step;
    }
}
