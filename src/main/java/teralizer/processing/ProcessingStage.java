package teralizer.processing;

public enum ProcessingStage {
    CLEANUP_PROJECT(0),
    BUILD_SPOON_MODEL(1),

    DOWNLOAD_PROJECT(2),
    SETUP_PROJECT(3),

    BUILD_PROJECT_ORIGINAL(4),
    GENERATE_TESTS(5),

    EXECUTE_TESTS_ORIGINAL(6),
    COLLECT_JUNIT_REPORTS_ORIGINAL(7),
    ANALYZE_TESTS(8),
    FILTER_TESTS(9),

    ADD_JPF_INSTRUMENTATION(10),
    BUILD_PROJECT_INSTRUMENTED(11),
    EXECUTE_JPF(12),
    CLEANUP_JPF_INSTRUMENTATION(13),

    ADD_DEPENDENCIES(14),
    BUILD_PROJECT_INITIAL(15),
    EXECUTE_TESTS_INITIAL(16),

    COLLECT_JUNIT_REPORTS_INITIAL(17),
    COLLECT_JACOCO_DATA_INITIAL(18),
    COLLECT_PIT_DATA_INITIAL(19),

    CLEANUP_GENERALIZATION(20),

    GENERALIZE_TESTS(21),
    BUILD_PROJECT_GENERALIZED(22),

    EXECUTE_TESTS_GENERALIZED(23),
    COLLECT_JUNIT_REPORTS_GENERALIZED(24),
    FILTER_GENERALIZATIONS(25),

    COLLECT_JACOCO_DATA_GENERALIZED(26),
    COLLECT_PIT_DATA_GENERALIZED(27);

    private final Integer step;

    ProcessingStage(Integer step) {
        this.step = step;
    }

    public Integer getStep() {
        return this.step;
    }
}
