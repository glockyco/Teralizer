package teralizer.processing;

public enum ProcessingStage {
    CLEANUP_PROJECT(0),

    DOWNLOAD_PROJECT(1),
    SETUP_PROJECT(2),

    BUILD_PROJECT_ORIGINAL(3),
    GENERATE_EVOSUITE_TESTS(4),
    POSTPROCESS_EVOSUITE_TESTS(5),

    BUILD_SPOON_MODEL(6),

    EXECUTE_TESTS_ORIGINAL(7),
    COLLECT_JUNIT_REPORTS_ORIGINAL(8),
    COLLECT_JACOCO_DATA_ORIGINAL(9),
    FILTER_TESTS_ORIGINAL(10),
    COLLECT_PIT_DATA_ORIGINAL(11),

    ANALYZE_TESTS(12),
    FILTER_TESTS(13),
    FILTER_ASSERTIONS(14),

    ADD_JPF_INSTRUMENTATION(15),
    BUILD_PROJECT_INSTRUMENTED(16),
    EXECUTE_JPF(17),
    ANALYZE_JPF(18),
    CLEANUP_JPF_INSTRUMENTATION(19),

    ADD_DEPENDENCIES(20),
    BUILD_PROJECT_INITIAL(21),
    EXECUTE_TESTS_INITIAL(22),

    COLLECT_JUNIT_REPORTS_INITIAL(23),
    COLLECT_JACOCO_DATA_INITIAL(24),
    COLLECT_PIT_DATA_INITIAL(25),

    CLEANUP_GENERALIZATION(26),

    GENERALIZE_TESTS(27),
    BUILD_PROJECT_GENERALIZED(28),

    EXECUTE_TESTS_GENERALIZED(29),
    COLLECT_JUNIT_REPORTS_GENERALIZED(30),
    FILTER_GENERALIZATIONS(31),

    COLLECT_JACOCO_DATA_GENERALIZED(32),
    COLLECT_PIT_DATA_GENERALIZED(33);

    private final Integer step;

    ProcessingStage(Integer step) {
        this.step = step;
    }

    public Integer getStep() {
        return this.step;
    }
}
