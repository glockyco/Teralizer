package teralizer.processing;

public enum ProcessingStage {
    CLEANUP_PROJECT(0),

    DOWNLOAD_PROJECT(1),
    SETUP_PROJECT(2),

    BUILD_PROJECT_ORIGINAL(3),
    GENERATE_EVOSUITE_TESTS(4),
    POSTPROCESS_EVOSUITE_TESTS(5),

    BUILD_SPOON_MODEL(6),

    EXECUTE_TESTS_ORIGINAL(7),
    COLLECT_JUNIT_REPORTS_ORIGINAL(8),
    COLLECT_JACOCO_DATA_ORIGINAL(9),
    COLLECT_PIT_DATA_ORIGINAL(10),

    ANALYZE_TESTS(11),
    FILTER_TESTS(12),
    FILTER_ASSERTIONS(13),

    ADD_JPF_INSTRUMENTATION(14),
    BUILD_PROJECT_INSTRUMENTED(15),
    EXECUTE_JPF(16),
    ANALYZE_JPF(17),
    CLEANUP_JPF_INSTRUMENTATION(18),

    ADD_DEPENDENCIES(19),
    BUILD_PROJECT_INITIAL(20),
    EXECUTE_TESTS_INITIAL(21),

    COLLECT_JUNIT_REPORTS_INITIAL(22),
    COLLECT_JACOCO_DATA_INITIAL(23),
    COLLECT_PIT_DATA_INITIAL(24),

    CLEANUP_GENERALIZATION(25),

    GENERALIZE_TESTS(26),
    BUILD_PROJECT_GENERALIZED(27),

    EXECUTE_TESTS_GENERALIZED(28),
    COLLECT_JUNIT_REPORTS_GENERALIZED(29),
    FILTER_GENERALIZATIONS(30),

    COLLECT_JACOCO_DATA_GENERALIZED(31),
    COLLECT_PIT_DATA_GENERALIZED(32);

    private final Integer step;

    ProcessingStage(Integer step) {
        this.step = step;
    }

    public Integer getStep() {
        return this.step;
    }
}
