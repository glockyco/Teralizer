package teralizer.processing;

public enum ProcessingStage {
    CLEANUP(null),

    PROJECT_DOWNLOAD(1),
    PROJECT_SETUP(2),
    PROJECT_BUILDING_ORIGINAL(3),
    TEST_EXECUTION_ORIGINAL(4),

    ADD_DEPENDENCIES(5),
    PROJECT_BUILDING_WITH_DEPENDENCIES(6),

    TEST_DETECTION(7),
    TEST_FILTERING(8),
    TEST_EXECUTION_FILTERED(9),

    TEST_DATA_COLLECTION_FILTERED(10),
    COVERAGE_DATA_COLLECTION_FILTERED(11),
    MUTATION_DATA_COLLECTION_FILTERED(12),

    JPF_INSTRUMENTATION(13),
    PROJECT_BUILDING_INSTRUMENTED(14),
    JPF_EXECUTION(15),

    TEST_GENERALIZATION(16),
    PROJECT_BUILDING_GENERALIZED(17),
    TEST_EXECUTION_GENERALIZED(18),

    TEST_DATA_COLLECTION_GENERALIZED(19),
    COVERAGE_DATA_COLLECTION_GENERALIZED(20),
    MUTATION_DATA_COLLECTION_GENERALIZED(21);

    private final Integer step;

    ProcessingStage(Integer step) {
        this.step = step;
    }

    public Integer getStep() {
        return this.step;
    }
}
