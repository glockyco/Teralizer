## 1. Establish the release input boundary

- [x] 1.1 Complete and validate tasks 5.9 through 6.5 of `consolidate-evaluation-databases`. Require its four-corpus manifest, clean import, preparation, read-only report run, and clean-tree checks before this release consumes it.
- [ ] 1.2 Finish, validate, sync, and archive `make-report-runs-explicit`, `separate-report-values-from-presentation`, `materialize-exclusion-evidence`, `declare-published-artifacts`, and `consolidate-repository-knowledge` in dependency order. Freeze the registered report run only after its Markdown, LaTeX, CSV, figure, macro, provenance, and claim-facing artifact set is final.
- [ ] 1.3 Inventory every version-2 source, nested source, corpus, report output, non-database report input, project, run-evidence, paper, license, container, and documentation path. Assign each path one component owner or one explicit non-payload disposition. Reject unowned, duplicated, ambient, or source-checkout-fallback inputs.
- [ ] 1.4 Prove report-input closure by resolving every registered `CorpusInputSpec` and `FileInputSpec`. Cover all four semantic corpora and `project-source-facts.json`, `jarvis-value-facts.json`, `cut_values.tsv`, and JARVIS completion evidence. Record each input's source, checksum, owner, and inspection-versus-regeneration status.
- [ ] 1.5 Close the JARVIS lineage across the two scorecard and twelve census fixtures, their configs and revisions, both JARVIS corpus dumps, status and provenance ledgers, compact value facts, all bound source value logs, CUT-PVC evidence, RQ0 artifacts, and the external JARVIS paper/table reference.
- [ ] 1.6 Reconcile the competing JARVIS completion paths and CUT source-cache paths. Replace the detached marker with one validated completion-evidence contract, use the fixture preparer's declared cache, rerun CUT-PVC capture from pinned fixtures, retain its raw TSVs, and verify both compact evidence files against their source checksums.
- [ ] 1.7 Prove source closure from the committed tree, tracked LFS objects, and required submodules. Materialize `jpf-symbc`, pinned `jpf-core`, and `projects/EqBench` at recorded commits; keep runtime source in core, keep controlled project input in `projects-controlled`, retain licenses, and reject gitlink-only or ambient worktree dependencies.
- [ ] 1.8 Add and validate the typed release declaration with release identity, DOI roles, paper identity, component ownership, workflow component sets, platforms, resources, network phases, claims, licenses, semantic corpus/report references, and production completeness rules.

## 2. Build manifest-declared component archives

- [ ] 2.1 Define three public, versioned document formats: release manifest, component manifest, and acceptance record. Keep provenance, claims, evidence lineage, and path dispositions as typed payload sections or build records instead of separate public schema families.
- [ ] 2.2 Implement an ordered source-to-archive plan from the release declaration. Validate safe paths, required checksums, unique wrapper roots, exactly one payload owner, explicit omitted-path dispositions, legal eligibility, and complete workflow dependencies before reading bulk payloads.
- [ ] 2.3 Measure candidate payloads and freeze a balanced component graph. Keep source, runtime support, registered results, compact inputs, and small backing evidence in `core`; separate the four corpus packages and the controlled, real-world sample, real-world remainder, JARVIS scorecard, and JARVIS census project families.
- [ ] 2.4 Build `core` with complete executable source and runtime inputs, tracked LFS content, required nested source, controller, locks, schemas, documentation, source provenance, registered reports, compact report inputs, selected JARVIS backing logs and captures, claims, and data dictionary. Do not copy corpus dumps or project trees into core.
- [ ] 2.5 Build one independently verified component for each semantic corpus: `controlled`, `real-world`, `jarvis-scenarios`, and `jarvis-benchmark`. Consume the corpus package manifest without duplicating corpus identity or physical database naming.
- [ ] 2.6 Build project components for controlled, real-world sample, non-overlapping real-world remainder, JARVIS scorecard, and JARVIS census inputs. Preserve revisions, configs, patches, provenance, and permitted licenses; prove the full real-world set contains no duplicated sample payload.
- [ ] 2.7 Implement a thin deterministic archive adapter over a maintained Zip64 library. Stream the ordered plan, normalize timestamps and permissions, preserve required executable bits, record hashes and sizes, and do not implement ZIP records or compression structures.
- [ ] 2.8 Verify every completed archive through an independent read path. Reject unsafe, missing, extra, duplicated, or checksum-mismatched members; verify its wrapper root, component manifest, release identity, and deterministic rebuild behavior.
- [ ] 2.9 Generate `release-manifest.json`, `checksums.sha256`, the release summary, Zenodo metadata, and exact upload inventory only after all declared archives verify in a fresh versioned candidate directory. Never scan old output or reuse cached archives automatically.
- [ ] 2.10 Replace `prepare-zenodo-package.sh` membership, wildcard checksum, copied-staging, and fixed-count logic with a thin release-builder invocation. Add synthetic fixtures for stale output, missing dependencies, duplicate owners, unresolved inputs, missing nested source, changed payloads, interrupted component writes, deterministic rebuilds, and failed candidate validation.

## 3. Install components without package-manager machinery

- [ ] 3.1 Implement component-local installation: verify one selected archive, extract it into a temporary private root, verify every payload byte, and rename only that component to `components/<component-id>` after success.
- [ ] 3.2 Make reinstalling an identical component a verified no-op. Reject a differing existing component with a corrective remove-or-new-workspace command. Keep no package database, global ownership ledger, workspace transaction log, or archive merge engine.
- [ ] 3.3 Resolve workflow prerequisites from verified component manifests and report the complete missing or incompatible set before execution. Require the sample plus non-overlapping remainder for full real-world collection and never infer readiness from nonempty shared directories.
- [ ] 3.4 Route existing corpus, report, and collection helpers to explicit component roots. Remove the `projects/`-nonempty and `data/`-nonempty shortcuts and prove that adding one component cannot skip, overwrite, or mutate another.
- [ ] 3.5 Put release identity, purpose, payload inventory, dependency list, citation, license mapping, schema or provenance context, and concise component-specific instructions in every independently downloadable archive. Test substitution, checksum failure, path traversal, existing-component disagreement, missing dependencies, and safe incremental extraction.

## 4. Preserve provenance and verify paper evidence

- [ ] 4.1 Generate embedded release provenance in a clean checkout with repository-relative producing-source paths, source hashes, last-changing commits, repository identity, and resolvable source URLs. Fail on dirty or unattributed producing sources.
- [ ] 4.2 Extend report provenance resolution with verified release mode that activates only from validated package metadata, checks producing-source bytes, and returns embedded per-file revisions without Git.
- [ ] 4.3 Test checkout and Git-free attribution equivalence. Reject absent records, mismatched source bytes, unrelated checkout commits, a fabricated common release commit, and an unverified Git-less directory.
- [ ] 4.4 Define the claims-to-evidence matrix for every represented RQ0-RQ6 claim. Include semantic and file inputs, exact command, artifact ids, expected values or invariants, tolerance, runtime, inspection-versus-regeneration status, and evidence class.
- [ ] 4.5 Audit RQ0 against the JARVIS publication. Bind its external table reference, fixture revisions, two JARVIS corpora, compact and raw value evidence, CUT-PVC evidence, completion/status evidence, and every report/table/CSV/macro/provenance artifact. State separately what report reproduction and each collection scope establish.
- [ ] 4.6 Mark every unsupported claim with its legal, empirical, or practical reason. Do not use a report name, frozen fact file, generic nondeterminism statement, or file presence as an acceptance rule.
- [ ] 4.7 Complete the report artifact manifest and deterministic comparison rules for every registered report, table, CSV, figure, macro, provenance record, and canonical figure source. Use explicit tolerance only for newly collected stochastic evidence.
- [ ] 4.8 Implement and test one manifest-driven evidence verifier. Detect missing, extra, and changed outputs; emit equivalent concise and machine-readable summaries; and fail after independent changes to each output kind, JARVIS input, claim expectation, and output membership.

## 5. Freeze the reviewer runtime and keep the controller thin

- [ ] 5.1 Pin every reviewer base and service image by digest, pin locked dependency resolution, remove permissive fallbacks, and record image architecture and provenance. Publish Dockerfiles and digest references; do not add an OCI archive in this release.
- [ ] 5.2 Make the analysis image run every report and verifier without host Python, `uv`, Nix, Git history, or a writable source tree. Expose only declared component, output, and disposable state mounts.
- [ ] 5.3 Fix Compose service and container-name disagreements, use service discovery instead of author deployment names, and prove corpus restore, preparation, analysis, and report-role checks under an arbitrary Compose project name.
- [ ] 5.4 Confine third-party Maven and Gradle execution to disposable scratch state with resource limits, no author credentials, no Docker socket, and no writable mount over immutable release components.
- [ ] 5.5 Add the root `artifact` controller with `preflight`, `smoke`, `reproduce-results`, `collect-reduced`, `collect-full`, and `clean`. Limit it to component checks, working-directory resolution, sequencing existing helpers, diagnostics, and summaries; add no status store, service manager, restorer, report registry, or checkpoint model.
- [ ] 5.6 Implement preflight for release and component identity, exact workflow component set, supported architecture, Docker and Compose compatibility, image digests, ports, memory, disk scope, and network phases. Report all missing prerequisites in one result.
- [ ] 5.7 Implement bounded smoke using core and the smallest useful declared corpus component. Verify packages and requirements, start services, restore and prepare the corpus, prove read-only report access, run one representative report, check expected evidence, and complete within the measured 30-minute baseline.
- [ ] 5.8 Route results reproduction through every registered report and the evidence verifier. Prove RQ0 uses only core plus its two JARVIS corpora, all-report reproduction uses core plus all four corpora, and both work without network after setup.
- [ ] 5.9 Route reduced and full collection directly through existing production stages, run ledgers, resource controls, and resume behavior. Exercise packaged controlled, real-world, scorecard, census, and CUT-PVC paths without adding controller-owned recovery state.

## 6. Complete reuse, licensing, and public documentation

- [ ] 6.1 Generate a checked data dictionary for published tables, columns, units, null meanings, derived views, result CSV fields, and provenance links. Do not require documentation for internal fields that no public workflow exposes.
- [ ] 6.2 Implement the third-party inventory gate for origin URL, immutable revision, detected license, retained license path and checksum, attribution, redistribution decision, and component membership.
- [ ] 6.3 Resolve redistribution records for every controlled, real-world sample, real-world remainder, JARVIS scorecard, and JARVIS census project input. Exclude source bytes with unclear permission and retain only legally permitted identity and retrieval information.
- [ ] 6.4 Resolve citation and licensing for JARVIS fixtures, selected value logs, CUT captures, `jpf-symbc`, `jpf-core`, external Table-2 values, and the JARVIS publication. Preserve applicable third-party notices and distinguish factual comparison data from copied source.
- [ ] 6.5 Audit generated tests, logs, database dumps, compact evidence, reports, and figures for copied third-party material, human-participant or sensitive data, and component-level licensing. Record each inclusion or exclusion decision.
- [ ] 6.6 Add the top-level license map, retained third-party license texts, notices, ethical statement, and explicit statement that analyzed projects keep their original licenses.
- [ ] 6.7 Generate stable documentation facts for component and workflow download sizes, unpacked and restored sizes, peak disk, memory, platform, network phase, runtime, DOI roles, commands, and archive names. Keep explanatory claims, limitations, security guidance, and external references hand-authored.
- [ ] 6.8 Document data inspection, schema interpretation, project and configuration addition, safe new-input collection, report extension, evidence export, stable interfaces, writable state, cleanup, network use, and untrusted-build risks.
- [ ] 6.9 Make every component independently understandable with a concise README, study and release identity, contents, dependencies, citation, licenses, provenance context, and applicable commands. Publish a workflow matrix with exact component lists and aggregate resource facts.
- [ ] 6.10 Correct citation, paper, DOI, JARVIS reference, badge, and Zenodo metadata. Validate only structured facts automatically: commands and options, component ids, archive names, DOI values, checksums, sizes, platforms, manifest paths, and internal links. Require human review for prose, claims, limitations, citations, and release presentation.

## 7. Prove the staged release at archive level

- [ ] 7.1 Add fast synthetic-package tests to pull-request CI for the three public document formats, archive build and verification, isolated component extraction, missing workflow dependencies, provenance release mode, structured documentation facts, and focused failures.
- [ ] 7.2 Add a release-candidate Linux x86-64 job that receives only staged upload files, starts in a new path with empty Docker volumes and no source checkout or author credentials, and retains its logs and acceptance record.
- [ ] 7.3 Record the release-manifest checksum, host and container architecture, image digests, selected workflow components, commands, outcomes, wall times, peak memory, peak disk by scope, network phases, and cleanup result in the acceptance record.
- [ ] 7.4 From staged components only, install and run the documented smoke set, then install the all-report set, reproduce every registered report through read-only roles, verify every supported claim and output category, run release-scoped cleanup, and repeat results reproduction without network access.
- [ ] 7.5 Make the source checkout, author `data/` and `projects/` roots, nested Git worktrees, and builder state inaccessible during acceptance. Verify source-log counts and hashes, completion/status evidence, CUT lineage, RQ0 artifacts, and inspection-versus-regeneration status from core and selected corpus components.
- [ ] 7.6 Before freezing the candidate, run one packaged reduced production unit for each materially different collection path. Keep interruption, retry, and resume coverage in the owning subsystem tests. Run Apple Silicon compatibility once only if the public release claims it.

## 8. Build and publish the immutable version

- [ ] 8.1 Create a draft successor under Zenodo concept DOI `10.5281/zenodo.17950380`, reserve its version DOI, and commit consistent version, citation, release declaration, and public metadata before the final build.
- [ ] 8.2 Build a provisional release from the verified four-corpus package, frozen report run, complete core inputs, and every declared large component. Require exact equality with the disposition ledger and capture measured archive, workflow, restore, resource, setup, reproduction, and cleanup facts.
- [ ] 8.3 Update measured release facts, regenerate structured documentation tables and Zenodo metadata, rebuild from a clean checkout in a fresh candidate directory, and reject every placeholder or superseded version-1 value.
- [ ] 8.4 Run the complete Linux archive acceptance gate against the exact final manifest and upload files, including smoke, all reports, JARVIS lineage and RQ0 checks, cleanup, no-network analysis, and failure diagnostics.
- [ ] 8.5 Obtain human review of component granularity, workflow download sets, paper-claim mappings, paper and JARVIS metadata, licenses, redistribution decisions, ethical statement, badge wording, DOI roles, limitations, and Zenodo presentation.
- [ ] 8.6 Run frozen repository tests, formatting, lint, types, commit hooks, strict OpenSpec validation, fixture CI, clean-tree checks, and the release-integrity validator without modifying the accepted upload set.
- [ ] 8.7 Upload the exact validated manifest, schemas, summaries, checksums, and component archives to the draft. Compare every remote file byte-for-byte with the accepted local inventory before publication.
- [ ] 8.8 After explicit approval, publish the new Zenodo version without modifying version DOI `10.5281/zenodo.18242626` or labeling the concept DOI as a version DOI.
- [ ] 8.9 Download the published release by version DOI into a clean location, verify all manifest and component checksums, rerun smoke and results reproduction, retain the post-publication acceptance record, update repository links and status, and confirm every public claim remains no stronger than the evidence.
