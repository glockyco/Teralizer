# Retired-source claim inventory

## Scope and decision rule

This is the one-time migration record required by `materialize-exclusion-evidence`. It reads the six files deleted by commit `642d615e3dabcc138bab7312bc0d700c261af0b5` from that commit's parent. The retired prose is a checklist, not evidence.

Each row records one normalized substantive claim. A row uses exactly one disposition:

- `accepted contract`: an accepted OpenSpec capability owns the rule.
- `executable checked fact`: current source and a focused check own the behavior.
- `registered empirical result`: a registered report and immutable provenance own the value.
- `deterministic qualitative evidence`: a reproducible fixture owns the explanation.
- `stale/disproved`: current source, tests, or accepted contracts contradict the retired claim.
- `intentionally discarded`: the material is volatile, duplicative, ownerless, or non-evidentiary.

This initial inventory assigns the maintenance owner and evidence class. Rows that name a proof requirement still require task 1.3 re-derivation before the final task 1.5 review. No runtime code reads this file.

## Prerequisite and owner boundaries

The prerequisite review found a clean producer worktree and no unarchived prerequisite change. The active `make-replication-artifact-badge-ready` change is a downstream consumer; it does not own producer semantics. The accepted prerequisite revisions are:

| Capability change | Accepted revision |
|---|---|
| `consolidate-evaluation-databases` | `edf5ae290a0659266fec28530c4873ab0db0a808` |
| `make-report-runs-explicit` | `6409d66588c271ffdcd4b75229319fa7459579da` |
| `separate-report-values-from-presentation` | `2cb26ea0f852c0163a0805dd06d464399e6787ee` |
| `consolidate-repository-knowledge` | `4042046a87e67048cdde506642320435a5865759` |
| `declare-published-artifacts` | `595db740d4a8e2c860d5e658e9e0755467c54a33` |

Each implementation subject has one authority. A claim row cites the authority key, not a second narrative owner.

| Key | Maintained owner | Authority | Executable evidence boundary |
|---|---|---|---|
| PXS | pipeline/cross-stage-contracts | openspec/specs/pipeline/cross-stage-contracts/spec.md | src/main/java/teralizer plus focused Java tests and verification fixtures |
| EXA | reporting/exclusion-accounting | openspec/specs/reporting/exclusion-accounting/spec.md | analysis/src/teralizer/eval/reports/rq6_causes.py, _funnel.py, and focused RQ6 tests |
| RIN | reporting/report-inputs | openspec/specs/reporting/report-inputs/spec.md | analysis/src/teralizer/corpora.py, eval/inputs.py, and report-input tests |
| REX | reporting/report-execution | openspec/specs/reporting/report-execution/spec.md | analysis/src/teralizer/eval/run.py and run/CLI tests |
| TRD | report-output/table-rendering | openspec/specs/report-output/table-rendering/spec.md | analysis/src/teralizer/eval/model.py, renderers, and rendering tests |
| APR | reporting/artifact-provenance | openspec/specs/reporting/artifact-provenance/spec.md | analysis/src/teralizer/eval/provenance.py and provenance tests |
| ADL | reporting/artifact-delivery | openspec/specs/reporting/artifact-delivery/spec.md | analysis/src/teralizer/eval/publish.py and publication tests |
| CRG | evaluation-data/corpus-registry | openspec/specs/evaluation-data/corpus-registry/spec.md | src/main/resources/db/corpora.toml, analysis/src/teralizer/corpora.py, and registry tests |
| DBL | evaluation-data/database-lifecycle | openspec/specs/evaluation-data/database-lifecycle/spec.md | scripts/lib/db-lifecycle.sh, analysis corpus preparation, and lifecycle tests |
| DBS | database DDL and derived views | src/main/resources/db/create-tables.sql and create-views.sql | generated jOOQ bindings plus persistence and view tests |
| VFX | verification fixture behavior | verification/fixtures and verification/golden | focused pipeline and golden verification checks |
| LOC | repository local-state safety | AGENTS.md and applicable scoped rules | runner-owned lifecycle scripts and focused safety checks |
| RQ6 | registered RQ6 report | analysis/src/teralizer/eval/registry.py and reports/rq6_causes.py | analysis/tests/eval/test_rq6_causes.py and test_rq6_invariants.py |
| MEE | materialize-exclusion-evidence change | openspec/changes/materialize-exclusion-evidence/specs/reporting/exclusion-evidence/spec.md | planned producer changes; not an accepted authority until archive |
| NONE | none | Historical source only | No current behavior depends on this claim |

`MEE` is not an accepted authority. It identifies a known gap that this active change must close. `NONE` means that the row is not retained as current behavior.

## Current misclassification check

The retired two-bucket mapping is stale. Current `analysis/src/teralizer/eval/reports/rq6_causes.py`:

- recognizes a filter rejection only when `filter_name` matches the filter-class pattern;
- classifies generation gates as `refused`;
- classifies inline capability exclusions as `unsupported`;
- classifies javac quarantine and task exceptions as `failed`.

On 2026-08-22, `nix develop --command uv run --directory analysis pytest tests/eval/test_rq6_invariants.py tests/eval/test_rq6_causes.py -q` completed with 20 passed tests and one strict expected failure. The expected failure is the separate missing attempt-state defect recorded by EXC-11 and EXC-22b. It is not the retired mechanism-classification bug. Task 2.5 still adds the isolated quarantine/filter-shape collision fixture required for the new canonical mapping.

## Claim inventory

Disposition totals: 52 accepted contract; 34 executable checked fact; 10 stale/disproved; 31 intentionally discarded; 2 deterministic qualitative evidence. No retired numeric result is retained as a registered empirical result: current publication must regenerate every retained value from the registered report and immutable run provenance.

| ID | Retired source and locator | Normalized claim | Disposition / evidence class | Owner | Current evidence, required proof, or discard rationale |
|---|---|---|---|---|---|
| ARC-01 | `architecture.md`: Processing Pipeline opening paragraph; Key components — PipelinePlanner, PipelinePhase, ProcessingPipeline | Selected generation, generalization, and reduction phases run independently in canonical order, clear only their own state, validate prerequisites, drain before handoff, and execute queued tasks by priority. | accepted contract | PXS | Check the current accepted pipeline contract, planner source, and phase-orchestration tests, including independent toggles, canonical ordering, phase-scoped clearing, missing-input failure, priority execution, and drain barriers. |
| ARC-02 | `architecture.md`: Processing Pipeline opening paragraph; Stages by phase — Bootstrap, Generalization, Reduction; Key components — PipelinePlanner/ProjectIdentity | Compatible persisted workspaces may be attached by root without fresh-only cleanup, and generalized variant sources are archived and restored per variant to support isolated later reduction. | accepted contract | PXS | Run or inspect current attach/resume and multi-variant reduction integration tests proving config-hash rejection, fresh-versus-attach cleanup behavior, persisted archive restoration, later-invocation reduction, and sibling isolation. |
| ARC-03 | `architecture.md`: Processing Pipeline opening paragraph; Key components — ProcessingPipeline | Variant task failures cancel only that variant’s queued work, shared or variant-null failures may cascade project-wide, and later reduction failure must preserve persisted generalization results. | accepted contract | PXS | Check scheduler failure-propagation tests for variant, shared, and variant-null tasks, plus an integration test showing retained generalization artifacts after reduction failure. |
| ARC-04 | `architecture.md`: Package responsibilities — processing.filter | Filtering is a total typed ACCEPT/REJECT/DEFER interface, with stable reason codes on every REJECT and DEFER. | accepted contract | EXA | Check the current FilterResult API, stable reason-code registry, exhaustive tests for all filter exits, and report ingestion tests showing no unclassified REJECT/DEFER outcome. |
| ARC-05 | `architecture.md`: Key components — MethodUnderTestResolver | MUT resolution is total and confidence-ranked, persists alternatives and deciding provenance, and populates tested-method identity only above the current generalization-grade threshold. | executable checked fact | PXS | Check current resolver source, schema/DDL, and resolver matrix tests for totality, ranking/alternatives, persisted provenance, and the tested-field population threshold; separately confirm whether historical T1–T5 labels remain current. |
| ARC-06 | `architecture.md`: Code generation — technology roles and snippet restrictions | Java requiring typing, reference resolution, or later AST processing must be built as Spoon AST; templates are only for textual artifacts, and snippets are restricted to opaque leaf identifiers. | accepted contract | PXS | Check current code-generation policy, source call sites, and structural AST tests or goldens proving that modeled Java uses AST nodes and snippets are restricted to permitted leaf references. |
| ARC-07 | `architecture.md`: Cross-stage contracts — GeneralizationRecipe | A persisted GeneralizationRecipe is derived once after MUT resolution and is consumed through one typed-failure resolver by instrumentation and generalization; consumers extend rather than re-derive it. | accepted contract | PXS | Check current accepted contract, recipe schema and serialization compatibility, producer/consumer source, typed-failure tests, and an end-to-end fixture proving both consumers use the same persisted recipe. |
| ARC-08 | `architecture.md`: Cross-stage contracts — Widening license | The current widening decision uses output class, literal-return evidence, concretization risk, and path-name coverage. SYMBOLIC and CONSTANT outputs widen; EXCEPTION and NULL_CONCRETE follow their checked evidence rules; refusals use typed codes. | executable checked fact | PXS | WideningLicense.evaluate and WideningLicenseTest; current accepted contract at openspec/specs/pipeline/cross-stage-contracts/spec.md. |
| ARC-08b | `architecture.md`: Cross-stage contracts — Evidence-bounded widening | The retired architecture restricted licensable NULL_CONCRETE output to boolean values represented in the path condition. | stale/disproved | PXS | WideningLicense.evaluate uses outputIsLiteral rather than return type, and WideningLicenseTest::nullConcreteLiteralSignalAllowsAnyReturnType proves the current rule. |
| ARC-09 | `architecture.md`: Cross-stage contracts — Extraction telemetry | SPF extraction persists output class and concretization provenance, treats native-boundary crossings as events, preserves box/unbox attributes and boxed-return extraction, and converts unsupported degradation to typed NULL_CONCRETE refusal. | accepted contract | PXS | Check current DDL/registry definitions, telemetry writers and readers, native-boundary instrumentation tests, box/unbox and boxed-return fixtures, and refusal-path tests demonstrating that degraded extraction cannot authorize widening. |
| ARC-10 | `architecture.md`: Cross-stage contracts — Sound SPF models in the fork | Supported SPF models for String.isEmpty, ASCII Character.isWhitespace, and numeric parseability intercept before native peers, produce no concretization events, and preserve their specified modeled constraints. | executable checked fact | PXS | Check current fork implementations and model-specific tests/fixtures for pre-peer interception, zero event counts, exact constraint forms, transformer ingestion, and generated-helper compilation/behavior. |
| ARC-11 | `architecture.md`: Cross-stage contracts — Inherited test methods | Inherited-test collection persists both declaring and JUnit child classes; flattening requires type-variable and private-access screens plus declaring imports, otherwise INHERITED_METHOD_NOT_FLATTENABLE is recorded. | executable checked fact | PXS | Check current schema semantics and inherited-method fixtures covering superclass resolution, child/declaring identity, both flattening screens, import merging, successful execution, and the typed exclusion path. |
| ARC-12 | `architecture.md`: Cross-stage contracts — Ingestion totality | SPF-term ingestion faithfully maps every admitted term or raises UnsupportedSpfTermException mapped to UNSUPPORTED_TERM; parent-linked string length is supported while unsupported indexOf/charAt-derived terms are refused. | accepted contract | PXS | Check exhaustive transformer dispatch tests, typed exception-to-exclusion mapping, parent-link serialization/round trips, length mapping, and refusal tests for every currently unsupported string-derived integer family. |
| ARC-13 | `architecture.md`: Cross-stage contracts — Build-file copies | The pipeline never mutates native project build files, uses tool-owned Maven/Gradle copies and a separate JUnit-platform-capable generalized-test configuration, and retains native runner configuration for original suites. | accepted contract | PXS | Check clean-worktree integration tests for Maven and Gradle, hashes/diffs proving native files remain unchanged, generated-copy configuration tests, and execution-path tests proving original versus generalized suites select the intended runner configuration. |
| ARC-14 | `architecture.md`: Cross-stage contracts — Generalization variants | Variants are profile-defined with no implicit defaults; BASELINE, NAIVE, and IMPROVED have distinct strategies, and generated jqwik properties use deterministic seed, ordering, no shrinking, seed-first execution, and draw deduplication. | accepted contract | PXS | Check current config schema/defaults and profile fixtures, algorithm-specific golden tests, and repeat-run determinism tests proving fixed seed, shrink policy, edge-case order, first seed tuple, and draw deduplication. |
| ARC-15 | `architecture.md`: Cross-stage contracts — Input generation | A type is generatable exactly when supported by a registered DomainPlanner; planners construct supported clauses and an unconditional residual filter enforces the remainder. | accepted contract | PXS | Check planner registry completeness and unsupported-type tests, per-clause construction tests, unconditional residual-filter tests, and verification-corpus cases where clauses are split between planner encoding and residual enforcement. |
| ARC-16 | `architecture.md`: Key Technical Distinction | SPF runs in constraint-collection mode over concrete executions, preserving input-output constraints without traditional symbolic path exploration. | executable checked fact | PXS | Check current JPF configuration, listener/control-flow source, and an execution fixture or trace showing concrete execution with constraint collection and no traditional path-search configuration. |
| ARC-17 | `architecture.md`: Verification | The retired assertion that current synthetic fixtures and observed goldens cover every retained architecture behavior is not retained as evidence. | intentionally discarded | NONE | Coverage must be shown by current focused tests and verification fixtures per behavior; a broad historical coverage statement proves nothing. |
| ARC-18 | `architecture.md`: Stages by phase (current ProcessingStage order), including Bootstrap/Generation/Generalization/Reduction numbered listings | Historical numeric stage ranges, stage labels, and point-in-time operation listings are discarded; only migrated ordering, barrier, persistence, and isolation contracts remain. | intentionally discarded | NONE | Editorial migration check only: ensure no unique phase or failure contract is lost and do not treat historical numbering/order inventories as current evidence. |
| ARC-19 | `architecture.md`: Package responsibilities; Key components | Historical package, component, path, ownership, and TaskContext-field inventories are discarded except for separately migrated normative mechanisms. | intentionally discarded | NONE | Editorial migration check that each substantive normative mechanism was split into its own entry; no verification of the historical package inventory is required. |
| ARC-20 | `architecture.md`: Dependencies — Key Dependencies | Historical dependency/tool inventories and local paths are discarded; current manifests, locks, and configuration are authoritative. | intentionally discarded | NONE | No migration evidence required; consult current manifests only when dependency information is needed. |
| ARC-21 | `architecture.md`: Code generation — Velocity templates paragraph | The ownerless future note about moving the Java value-recorder harness to a precompiled support jar is discarded and is neither a current contract nor evidence that the move occurred. | intentionally discarded | NONE | No product verification required; if the proposal still matters, create a separately owned and accepted design/change record. |
| ARC-22 | `architecture.md`: Code generation introductory cross-reference; Verification workflow references; parenthetical source locators throughout; final Wall time | Worked-example pointers, source maps, cross-references, navigation locators, repeated explanations, and recorded wall time are discarded as non-evidentiary metadata. | intentionally discarded | NONE | Editorial check only that no unique substantive claim is hidden in the removed navigation or repetition; current accepted specs, source, tests, and CI remain the required evidence. |
| ART-01 | `artifacts.md`: Opening paragraphs and the four artifact-family sections | The expression-slice fixture exercises one generated artifact family end to end across instrumentation, driver/JPF binding, extraction, and a generalized jqwik property. | executable checked fact | PXS | Regenerate the expression-slice fixture in a clean environment and assert that the checked golden contains all artifact roles and that the integration test consumes them end to end. The retired prose is only a locator, not evidence. |
| ART-02 | `artifacts.md`: Instrumented class section, first prose paragraph | Expression-recipe instrumentation isolates the selected test, lifts recipe input sites to wrapper parameters, places the asserted expression in the wrapper, and rewrites the assertion to call it. | executable checked fact | PXS | Use an instrumentation test that parses or compiles regenerated output and checks test isolation, wrapper parameters, expression lifting, and assertion rewriting together. |
| ART-03 | `artifacts.md`: Instrumented class section, contrast and instance-method paragraphs | Invocation instrumentation wraps only the call, derives return type from assertion context, passes instance receivers as _target_, and passes referenced locals as concrete _local_* parameters. | executable checked fact | PXS | Run focused fixtures for invocation, instance-receiver, and referenced-local cases; compile the outputs and assert the wrapper signature, return type, and concrete auxiliary arguments. One parameterized variant suite can verify these coupled rules. |
| ART-04 | `artifacts.md`: Driver class section, explanatory paragraph | The generated driver instantiates the instrumented test, runs applicable setup methods, and invokes the isolated test so collection observes the concrete wrapper path. | executable checked fact | PXS | Inspect regenerated driver structure and execute integration cases both with and without setup methods; verify that the selected wrapper path is reached and produces the expected extraction event. |
| ART-05 | `artifacts.md`: JPF config section, load-bearing-lines paragraph | The JPF binding makes lifted inputs symbolic, keeps receiver/local support parameters concrete, collects on one concrete path, and captures at wrapper-frame exit. | executable checked fact | PXS | Regenerate and parse the config, then run an integration test that verifies symbolic versus concrete parameter treatment, single-concrete-path collection behavior, and capture at the intended frame exit. Do not infer these semantics from property names alone. |
| ART-06 | `artifacts.md`: JPF config section, paragraph after the properties listing | Extraction emits adjacent concrete-input, concrete-output, symbolic-input path-condition, and symbolic-output specifications, using null when no symbolic return attribute survives. | executable checked fact | PXS | Execute extraction cases with and without a surviving symbolic return attribute; schema-validate all four outputs and check their semantic contents against listener tests and the accepted cross-stage definitions. |
| ART-07 | `artifacts.md`: Generalized test section, opening paragraph and sentence after the property listing | Generalization clones the test and replaces the selected assertion with a jqwik property that retains the asserted expression while replacing generated input sites with parameter-object reads. | executable checked fact | PXS | Regenerate, AST-compare the selected expression before and after site substitution, ensure unrelated transformation is absent, and compile and run the resulting property. |
| ART-08 | `artifacts.md`: Generalized test section, property annotation listing | The shown fixture’s property requests the captured tuple first, uses deterministic no-shrink generation, applies a bounded filter-miss hook, and requests 100 tries. | executable checked fact | PXS | Parse the regenerated annotation and run the property under the supported jqwik version to confirm the effective edge-case ordering, seed/determinism, shrinking mode, try count, and filter-miss hook behavior. Treat exact defaults as current checked behavior, not a durable contract from this document. |
| ART-09 | `artifacts.md`: Generalized test section, supplier explanation and supplier listing | Generated suppliers construct planner-supported clauses and residually filter the remainder; in the expression fixture, a variable comparison becomes a dependent bound while the full predicate is rechecked. | executable checked fact | PXS | Regenerate the fixture, inspect the supplier AST or model, and use generated-value tests to prove both the dependent bound and residual predicate are present and jointly admit only satisfying tuples. Also compare planner accounting with the extracted path condition. |
| ART-10 | `artifacts.md`: Generalized test section, support-class bullet for TestParameters | The generated property receives a parameter carrier with one public field per generated input site. | executable checked fact | PXS | Compile regenerated output and reflect on or AST-check the carrier fields and property parameter type for fixtures with different site counts and types. |
| ART-11 | `artifacts.md`: Generalized test section, support-class bullet for FirstValueArbitrary | FirstValueArbitrary emits the captured tuple first, deduplicates later draws, and overrides both generator overloads to prevent edge-case reinjection. | executable checked fact | PXS | Under the currently supported jqwik version, invoke both generator overloads and assert first-edge-case emission and deduplication across the complete generated sequence, including a regression case for edge-case reinjection. |
| ART-12 | `artifacts.md`: Generalized test section, support-class bullet for JqwikValueRecorder | Generated tests currently inline a telemetry harness that writes value and outcome data and remaps filter exhaustion through a lifecycle hook. | executable checked fact | PXS | Run passing, failing, and filter-exhaustion properties; verify value rows, outcome sidecar contents, and remapped outcomes against consumer schemas. Separately confirm whether the harness remains inlined in current regenerated output. |
| ART-13 | `artifacts.md`: Generalized test section, final sentence of the JqwikValueRecorder bullet | The retired source says a separate accepted specification requires moving the inline telemetry harness into a precompiled jar. | stale/disproved | NONE | No current or accepted OpenSpec owns this change; openspec/changes/archive/2026-08-22-consolidate-planning-home/disposition.md records the draft as ownerless. |
| ART-14 | `artifacts.md`: Opening paragraphs and artifact section headings | The fixture’s exact assertion, identifier, command, paths, stage presentation, and globs are discarded as historical run metadata and navigation. | intentionally discarded | NONE | Confirm that no accepted contract references these exact identifiers or paths. Current commands and locations should be discovered from maintained runner help and fixture configuration, not copied from this retired file. |
| ART-15 | `artifacts.md`: Java listings in the Instrumented class, Driver class, and Generalized test sections | Verbatim generated Java snippets, including exact names, literals, formatting, casts, and omissions, are deliberately not migrated as claims. | intentionally discarded | NONE | Ensure the retained mechanism entries are covered by executable checks, then rely on a fresh regeneration for exact source text. No historical snippet comparison is admissible as evidence. |
| ART-16 | `artifacts.md`: JPF config section, properties block and trimmed marker | The verbatim historical JPF properties listing is deliberately not migrated as a contract; only separately specified semantic configuration rules remain. | intentionally discarded | NONE | Verify current config through template tests and an executable JPF run; preserve semantic rules through the dedicated config-mechanism entry rather than retaining this listing. |
| ART-17 | `artifacts.md`: Instrumented class final parenthetical, JPF output paragraph cross-reference, and Generalized test support-class bullets | The support-class inventory, approximate harness size, source pointers, cross-references, and deduplication-discovery anecdote are discarded as volatile explanatory metadata. | intentionally discarded | NONE | Check that every substantive mechanism mentioned in this metadata is represented by another checklist entry. Do not preserve approximate size, discovery provenance, or source/line navigation as evidence. |
| ART-18 | `artifacts.md`: Reading the family against the DB section | Exact historical generalization and property-execution database row values are discarded as point-in-time observations. | intentionally discarded | NONE | If these values are needed, regenerate the fixture and query schema-validated current results. Do not use the retired row transcription as evidence or promote its counts into a contract. |
| DAT-01 | `database.md`: Database Structure opening sentence; Analysis Views opening sentence | Only maintained DDL and view specifications are authoritative for the relational schema; the retired database narrative is not. | accepted contract | DBS | Check the current DDL, view definitions, migration process, and schema-validation tests. Confirm the accepted pipeline/cross-stage contracts point to those artifacts rather than to this document. |
| DAT-02 | `database.md`: Core Tables — project | Persisted per-project controls independently gate generation, generalization, and reduction and must be enforced by the pipeline subject to cross-stage prerequisites. | accepted contract | PXS | Run or inspect tests proving each current phase control independently gates the corresponding phase and cannot bypass cross-stage prerequisites. Verify current field names from source/DDL rather than retaining the historical names as authority. |
| DAT-03 | `database.md`: Core Tables — assertion | Assertion persistence stores resolved MUT data, output classification, concretization telemetry, and a recipe interpreted consistently by instrumentation and generation. | accepted contract | PXS | Check current DDL and writer/reader source, then run an end-to-end persistence test proving one discovered assertion’s recipe and telemetry survive storage and are interpreted consistently by both downstream consumers. |
| DAT-04 | `database.md`: Core Tables — assertion_semantics | Discovery determines and persists assertion semantic family, argument shape, and failure context. | executable checked fact | PXS | Inspect current discovery writers and DDL, and execute representative tests showing semantics, argument shape, and failure context are written at discovery time. |
| DAT-05 | `database.md`: Core Tables — mut_resolution_observation | Every assertion, including unresolved ones, has persisted MUT-resolution observations containing provenance, confidence, candidates, and input topology. | executable checked fact | PXS | Use current source and integration tests to prove both resolved and unresolved assertions receive observations with the required provenance/topology fields; confirm coverage with database constraints or completeness tests. |
| DAT-06 | `database.md`: Core Tables — generalization | Generation exclusions persist both a broad typed label and the specific evidence-bounded widening refusal code; the broad label alone is insufficient for branch accounting. | accepted contract | EXA | Check the accepted exclusion taxonomy and current gate source. Run tests for every current widening-refusal branch, proving the broad exclusion and branch-specific code are both persisted and reportable. |
| DAT-07 | `database.md`: Core Tables — generalization_lifecycle | Lifecycle rows, not inclusion flags, measure emitted-test yield; pre-emission refusals have no lifecycle row, and a first-incomplete final stage alone cannot distinguish failure from non-execution. | accepted contract | EXA | Run end-to-end fixtures covering pre-emission refusal, emitted success, emitted stage failure, and an unrun downstream stage. Verify lifecycle population, final usability, failure coding, denominator interpretation, and report queries against the accepted contract. |
| DAT-08 | `database.md`: Core Tables — generation_clause / generation_parameter | Generation telemetry records canonical clause shape and consumption and each parameter’s symbolic-spec presence and representation, enabling recipe-consumption checks. | executable checked fact | PXS | Execute a fixture with consumed and unconsumed clauses and multiple parameter representations; compare emitted telemetry with the recipe and current DDL constraints. |
| DAT-09 | `database.md`: Data Collection Tables — jqwik_execution_run / jqwik_property_execution | Property executions persist diagnostic classification, attempts, tuple diversity, seed, and diagnostic-sidecar provenance. | executable checked fact | PXS | Run fixtures for successful execution, assertion failure, filtering limitation/exhaustion, and missing diagnostics. Verify classifications and quantitative/provenance fields are persisted from runner output. |
| DAT-10 | `database.md`: Data Collection Tables — jpf_extraction_summary | Extraction telemetry rolls each test up from scheduling through instrumentation, extraction, and specification writing, grouping failures by stable causes. | executable checked fact | PXS | Run success and representative failure fixtures; reconcile event-level execution with the persisted rollup and validate that failure causes come from the current stable registry. |
| DAT-11 | `database.md`: Data Collection Tables — task_diagnostic | Failed tasks persist stable reason codes and structured diagnostics, including compiler details, so analysis need not parse free-text logs. | executable checked fact | PXS | Trigger representative task and compilation failures and prove structured rows are emitted with registered reason codes and details; inspect current analysis/report code to confirm it does not use log-text parsing for these classifications. |
| DAT-12 | `database.md`: Data Collection Tables — build_environment_observation | Build stages record effective build/compiler configuration and generated-source language-feature observations needed to explain stage-specific compilation. | executable checked fact | PXS | Execute builds with controlled compiler settings and generated-source features, then compare observed records with the actual invocations and source scan. Confirm which stages are required to emit observations. |
| DAT-13 | `database.md`: Data Collection Tables — filter_result | Filter-decision storage includes adjudicated filter entities but may also contain compiler-quarantine verdicts from a non-filter producer, so a rejection value alone does not prove filter rejection. | accepted contract | EXA | Use fixtures that produce true filter decisions and compiler-quarantine verdicts. Prove current reports classify by producer/mechanism and adjudicated population, not by rejection value alone. |
| DAT-14 | `database.md`: Analysis Views — project success milestones and compatibility alias | Analysis views distinguish generalized-filter completion from reduction evidenced by mutation rows, while the legacy success view aliases the reduction milestone. | executable checked fact | DBS | Inspect current view SQL and execute seeded database fixtures for each milestone. Confirm membership semantics and alias equivalence without carrying forward historical stage numbers. |
| DAT-15 | `database.md`: Database Configuration — published-paper corpus and canonical RQ6 source claims | Canonical corpus and snapshot identity comes from the current evaluation registry and registered runner/report inputs, not database names or informal labels. | accepted contract | CRG | Resolve the currently registered corpus and immutable snapshot from the registry, verify the runner consumes that identity, and check completeness/provenance hashes before accepting any empirical analysis. |
| DAT-16 | `database.md`: Database Configuration — cross-DB comparisons | Cross-database reconciliation uses the registered stable project identity, historically root path, and never database-local numeric IDs. | accepted contract | CRG | Confirm the registry’s current stable key and uniqueness/normalization rules. Run a cross-database fixture where local IDs differ and prove analysis joins only through the registered identity. |
| DAT-17 | `database.md`: Database Configuration — protected corpora and scratch databases; Data directory layout pointer | Protected evaluation and published-result stores are not experimentation targets; destructive work is confined to explicitly bounded scratch state. | accepted contract | DBL | Check current local-state guidance and runner code for allowlists, read-only protections, scope checks, and cleanup behavior. Exercise safeguards against a protected target and lifecycle tests against a scratch target. |
| DAT-18 | `database.md`: Core Tables and Data Collection Tables — table/column listings, including project/test/task, JUnit, coverage, and mutation report inventories | Historical table/column inventories, enum lists, and generic metadata descriptions are discarded in favor of current DDL and generated schema documentation. | intentionally discarded | NONE | Confirm every still-used object is represented in current DDL/migrations and that retained semantic mechanisms have dedicated entries/tests above. No historical list should be used as completeness evidence. |
| DAT-19 | `database.md`: Database Configuration — named PostgreSQL databases, corpus mappings, count, and earlier corpus list | Historical database names, corpus mappings, project counts, and superseded-database inventories are discarded as point-in-time metadata. | intentionally discarded | NONE | Verify the current registry contains authoritative corpus/snapshot identity and counts derived from the registered snapshot; ensure reports do not cite the retired names or count. |
| DAT-20 | `database.md`: Database Configuration — postgres_reporeapers_rq6_v6 canonical/default assertion | The claim that the version-6 RepoReapers database is the current canonical RQ6 source and runner default is stale and not carried forward. | stale/disproved | CRG | Inspect the current registry and runner defaults and record the actual registered snapshot. Add or run a test that rejects an unregistered or legacy default. |
| DAT-21 | `database.md`: Analysis Views — Stage 4/Stage 5 labels; Database Structure and Analysis Views — source paths | Historical stage numbers attached to view milestones and source/path pointers are discarded as navigation rather than contract content. | intentionally discarded | NONE | Confirm retained milestone tests use semantic states rather than numeric stages, and that current ownership links are discoverable from maintained specs/build configuration. |
| DAT-22 | `database.md`: Database Operations; Database Configuration — centralized config; Data directory layout | Historical commands, UI endpoint or credentials, configuration function names, and local-directory pointers are discarded as volatile operational details. | intentionally discarded | NONE | Ensure maintained operational documentation and configuration tests provide current procedures without copying commands, credentials, function names, or paths from this retired document. |
| EXC-01 | `exclusion-model.md`: “The short version”; “Per-level composition in v6”; “Known defects” (structural root-cause paragraph) | Exclusion accounting totally classifies entities by semantic mechanism—filter rejection, build quarantine, generation gate, inline capability exclusion, or task exception—rather than storage location, preserving candidate-declination versus failure semantics. | accepted contract | EXA | Check the current classifier and report against a fixture containing all five mechanisms at every applicable entity level. Prove that classification is total, does not use a silent ELSE/fall-through, and assigns javac quarantine and inline capability exclusion by semantics rather than storage location. |
| EXC-02 | `exclusion-model.md`: “Mechanism reference” → “1. Filter rejection” | All configured filters run without short-circuiting, each records a decision at the most specific entity scope, any REJECT excludes, and overlapping rejection rows must be deduplicated for unique counts. | executable checked fact | EXA | Inspect current filter-task source and run unit/integration tests with multiple accepting, deferring, and rejecting filters. Verify all filters execute, rows overlap as claimed, entity scoping is correct, and report queries deduplicate entities rather than summing rejection rows. |
| EXC-03 | `exclusion-model.md`: “Mechanism reference” → “2. Build quarantine”; “Column semantics” → “filter_result” | Build quarantine is a compilation failure, not a filter: an offending unit may be removed and recorded through a registered non-filter filter_result identity, while accounting still reports failure even if the containing build later succeeds. | accepted contract | EXA | Run compilation-quarantine integration tests for instrumented wrappers and generalized tests. Confirm the current marker and reason codes are registered, the bad unit is omitted, the build outcome is represented correctly, and the report classifies the entity as a failure without treating the marker as a real filter. |
| EXC-04 | `exclusion-model.md`: “Mechanism reference” → “3. Generation-time gate”; “Invariants” items 1–2 | The ordered generation gate runs before source emission; refusal records the first typed failed check and creates no source, lifecycle row, or filter-result row. | accepted contract | EXA | Use parameterized integration tests for each gate and for a case failing multiple gates. Verify no source, lifecycle row, or filter row is created on refusal; verify emitted attempts do create lifecycle state; and verify first-check attribution matches the documented current order. |
| EXC-05 | `exclusion-model.md`: “Mechanism reference” → “4. Inline capability exclusion” | Inline capability exclusion records an unsupported shape as a typed candidate declination without a filter row and remains distinguishable from actual filtering. | accepted contract | EXA | Exercise each currently supported inline-capability exclusion path, including inherited-method handling and any active postprocessing path. Confirm typed registration, absence of filter rows, correct entity scope, and classification as unsupported/candidate-declination rather than failure. |
| EXC-06 | `exclusion-model.md`: “Mechanism reference” → “5. Task exception”; “Known defects” EM-5 and EM-6 | Task exceptions produce structured diagnostics, FAILED status, and consistently scoped lifecycle failure; entity/project fan-out must agree and fatal Throwables must not leave success state. | accepted contract | PXS | Inspect current catch boundaries and run fault-injection tests for Exception, Error/Throwable, entity-scoped failure, and project-scoped failure. Verify task status, diagnostics, affected-record state, and lifecycle fan-out agree. Do not accept the retired EM-5/EM-6 status text as proof that fixes remain present. |
| EXC-07 | `exclusion-model.md`: “The generalization funnel” → “Three denominators, three different questions” | Generalization attempts, emitted tests, and filter-adjudicated emitted tests are distinct denominators, and every rate must identify which population it uses. | accepted contract | EXA | Define the three populations in the current registry/report contract and test them on a fixture containing pre-emission refusals, pre-filter failures, rejected emitted tests, and passes. Verify report labels and generated prose/macros carry the denominator identity. |
| EXC-08 | `exclusion-model.md`: “Column semantics” → “is_included — never use as a success signal” | is_included is not validated success; lifecycle state, especially generated_filter_passed, owns generalization yield, and complete mechanism accounting is required at test and assertion levels. | accepted contract | EXA | Check current DDL defaults, every exclusion/failure update path, lifecycle views, and report SQL. Add fixtures where processing fails without a normal filter rejection and prove that no report labels such rows successful solely because is_included remains true. |
| EXC-09 | `exclusion-model.md`: “Column semantics” → “exclusion_info” | Exclusion provenance uses registered parseable mechanism/reason codes and structured crash diagnostics; reports must not infer identity from accidental free-form formatting. | accepted contract | EXA | Inventory current writers and parsers from source and schema, then test one fixture per provenance shape. Confirm every emitted code/shape is registered and unambiguous. If legacy free-form rows remain supported, specify and test an explicit compatibility parser rather than treating this document’s examples as evidence. |
| EXC-10 | `exclusion-model.md`: “Column semantics” → “generalization_lifecycle”; “The generalization funnel” | generalization_lifecycle has one row per emitted test; generated_filter_passed measures generalization success, while final_usable additionally requires downstream mutation collection. | accepted contract | EXA | Check lifecycle creation and flag transitions with integration tests spanning emission, pre-filter failure, filter rejection/pass, and downstream PIT success/failure. Verify report measures select generated_filter_passed or final_usable according to the stated claim. |
| EXC-11 | `exclusion-model.md`: “Column semantics” → “generalization_lifecycle” (deriveRollup limitation); “Invariants” item 7 | The retired source says lifecycle rollups distinguish an attempted failure from a stage that never ran. | stale/disproved | MEE | analysis/tests/eval/test_rq6_invariants.py::test_lifecycle_failure_stage_was_actually_attempted is strict xfail; the current rollup uses the first unset flag. |
| EXC-12 | `exclusion-model.md`: “The widening gate” → “The question it asks” | Generated-input filtering renders the persisted flattened path predicate, so widening licensing concerns oracle support under widened inputs rather than independently redefining the admitted input region. | accepted contract | PXS | Trace the current persisted predicate through both generation algorithms into regenerated source, and add golden/integration tests showing that surviving generated inputs satisfy the same predicate. Separately assert that WideningLicense consumes oracle evidence rather than silently changing the admitted input region. |
| EXC-13 | `exclusion-model.md`: “The widening gate” → “What licenses a widening” (output-model table and guard discussion) | Widening decisions exhaustively follow persisted output class: SYMBOLIC and CONSTANT are licensed, while EXCEPTION and NULL_CONCRETE require additional evidence. | accepted contract | PXS | Verify the current output-model enum and persisted representation, then run one decision test per class. Add exhaustiveness checks so a new class cannot inherit a default verdict or become unreportable. |
| EXC-14a | `exclusion-model.md`: exclusion-model.md — “The widening gate” → “What licenses a widening”; “Why each refusal happens” | For EXCEPTION output, an empty path condition can license an unconditional throw; otherwise the path must cover every widened parameter, and concretization with unknown or positive divergence risk is refused. | executable checked fact | PXS | WideningLicense.evaluate and the exception cases in WideningLicenseTest. |
| EXC-14b | `exclusion-model.md`: “The widening gate” → “What licenses a widening”; “Why each refusal happens” | For NULL_CONCRETE output, widening requires a literal-return signal, no concretization events, nonempty widened and path-name sets, and path coverage of every widened parameter; an empty path condition is insufficient. | executable checked fact | PXS | WideningLicense.evaluate, WideningLicenseTest, and NullConcreteRefusalShapeTest. |
| EXC-14c | `exclusion-model.md`: “The widening gate” → “What licenses a widening”; “Why each refusal happens” | The retired source restricted the NULL_CONCRETE rule to boolean or boxed-boolean oracle expressions. | stale/disproved | PXS | WideningLicense evaluates the literal-return signal without a return-type check; WideningLicenseTest proves that the signal applies to any return type. |
| EXC-15 | `exclusion-model.md`: “The widening gate” → “Why each refusal happens” → “The path condition does not pin every generated parameter” | Coverage includes all generated parameters and supported temporary variables against flattened conjunct names, with distinct refusal reasons for empty generated sets, empty path sets, and partial coverage. | accepted contract | PXS | Create fixtures with declared parameters, temporary constructor/model variables, unsupported temporary types, empty sets, and partially covered sets. Verify the generated and path-condition sets, the license verdict, and the persisted refusal reason together. |
| EXC-16 | `exclusion-model.md`: “The widening gate” → “Refusal causes measured” | Every widening refusal persists the first ordered license reason, and implementation, registry, and report-label vocabularies must remain exhaustive and drift-failing. | accepted contract | EXA | Compare the current implementation’s declared code set with schema/registry and report labels. Use multi-cause fixtures to verify first-match attribution, and retain a test that fails for either an unlabelled implementation code or an impossible report-only code. |
| EXC-17 | `exclusion-model.md`: “The widening gate” → “The residual risk the gate accepts” | The retired source says an unrelated path-condition branch can license a pass-through boolean oracle and leaves a residual false-license case for downstream validation. | stale/disproved | PXS | Current WideningLicense requires outputIsLiteral for NULL_CONCRETE; WideningLicenseTest refuses a field-read/pass-through boolean despite path evidence. |
| EXC-18 | `exclusion-model.md`: “The widening gate” → “Why the output model is missing” (intro and “Computed booleans”) | A computed boolean can reach NULL_CONCRETE because its bytecode literal return has no expression attribute while the input/result relation remains in the path condition; NULL_CONCRETE does not by itself mean semantic constancy. | deterministic qualitative evidence | PXS | NullConcreteRefusalShapeTest and OutputSpecClassifierTest reproduce the direct-comparison, local-store, and concrete-return shapes on the pinned JPF/SPF stack. |
| EXC-19 | `exclusion-model.md`: “The widening gate” → “Why the output model is missing” → “Heap reads” | The retired source says ordinary heap-derived outputs are not recovered without symbolic heap initialization and that field variables necessarily remain unbound. | stale/disproved | PXS | NullConcreteRefusalShapeTest proves field and array round trips preserve symbolic expressions through current jpf-core heap transfers. |
| EXC-20 | `exclusion-model.md`: “The widening gate” → “Why the output model is missing” → “Callee returns”, “Loop accumulation”, and “Concretization”; “Why each refusal happens” → concretization discussion | Ordinary callee returns, computed literals, loop accumulation, and native/concretization boundaries have distinct output-evidence behavior and must not be collapsed into one NULL_CONCRETE cause. | deterministic qualitative evidence | PXS | NullConcreteRefusalShapeTest, DivergenceRiskCaptureTest, and TestGeneralizationListenerConcretizationTest provide current deterministic characterization fixtures. |
| EXC-21 | `exclusion-model.md`: “The widening gate” → source-audit paragraph under “What licenses a widening”; “Why the output model is missing” → 25-case audit references | The retired stratified source audit is not retained as an empirical result because no registered immutable audit input with row identities, strata, decisions, and reviewer protocol is available. | intentionally discarded | NONE | Current report inputs and provenance contain no registered audit package for the historical sample; source prose is not evidence. |
| EXC-22a | `exclusion-model.md`: “Invariants”; “Known defects” concluding paragraphs | RQ6 accounting must fail on unknown filter writers or typed exclusion codes, partition attempts into refused and emitted outcomes, keep refused attempts free of downstream rows, partition each entity level, and reject a filter-passed lifecycle row with a REJECT decision. | executable checked fact | EXA | `test_rq6_invariants.py` and `test_generalization_funnel.py` pass the mechanism, lifecycle, and denominator invariants against the registered corpus. |
| EXC-22b | `exclusion-model.md`: “Invariants”; “Known defects” concluding paragraphs | The retired source says every reported lifecycle failure stage is known to have been attempted. | stale/disproved | MEE | `_generalization_funnel.py` checks independent task records and emits unknown attempt state. `test_generalization_funnel.py` proves that later failure labels do not become attempt evidence. |
| EXC-23 | `exclusion-model.md`: Opening measurement paragraph; all v6 count tables; “The generalization funnel”; “Per-level composition in v6”; quantitative paragraphs throughout “The widening gate” | Historical corpus names, counts, percentages, yields, and v6/v7 populations are discarded and must be regenerated from a registered report and immutable snapshot. | intentionally discarded | NONE | Confirm any still-cited quantitative result is produced by the current registered runner from a named immutable snapshot with completeness checks. Remove copied numbers and database names from the migrated contract. |
| EXC-24 | `exclusion-model.md`: All inline source-line references; “Column semantics” regex snippet; “Re-deriving these numbers”; scattered code and macro-name listings | Historical SQL, commands, snippets, regexes, file/line citations, source maps, and duplicate derivations are discarded as evidence and contract text. | intentionally discarded | NONE | Ensure the migrated owners expose executable tests or registered report definitions for each retained claim. Keep snippets only in live implementation documentation where they are tested; do not copy them into the durable migration artifact. |
| EXC-25 | `exclusion-model.md`: “Known defects” table and follow-up discussion | The EM-1–EM-9 ledger, historical counts/statuses, and embedded experiments are discarded as current truth; live issues, contracts, and tests own remaining hazards. | intentionally discarded | NONE | Search the current issue tracker and source/tests for each safety-relevant mechanism, especially failure scoping and attempted-versus-failed lifecycle semantics. Create or link live issues for unresolved behavior, then retire the historical IDs and measurements. |
| EXC-26 | `exclusion-model.md`: Title/preamble; repeated summaries in “The short version”, “Per-level composition in v6”, and “Known defects”; “Refusal causes measured” chapter commentary; “See also” | Document scaffolding, repeated explanations, navigation, manuscript layout commentary, macro/table presentation, old stage labels, and hand-maintained inventories are discarded. | intentionally discarded | NONE | Review the migrated documents for duplicated normative text or copied inventories. Retain only links to the authoritative contracts/report and ensure no old stage number, chapter-layout detail, or macro name is treated as a durable semantic claim. |
| LOC-01 | `local-state.md`: Opening paragraphs and “Rules that keep this doc from going stale” bullets | The retired local-state file’s authority and update-governance assertions are discarded; current scoped guidance and executable owners define safety boundaries. | intentionally discarded | NONE | Confirm that current scoped guidance identifies authoritative safety boundaries and that no accepted cross-stage contract still names local-state.md as authority; the retired text itself is not evidence. |
| LOC-02 | `local-state.md`: “Run outputs under data/” introductory sentence and parenthetical details in its table | Historical run-root layout, ledgers, logs, resume markers, environment names, default paths, and lane aliases are discarded as a point-in-time inventory. | intentionally discarded | NONE | No behavioral verification is required for migration; ensure any still-needed operator details are generated from current CLI/configuration source rather than copied from this inventory. |
| LOC-03 | `local-state.md`: “Run outputs under data/” table, Reporeapers-lane run-roots row | A measurement run root and snapshot database are one retention unit, while a scratch lane root and database are one deletion unit; measurement snapshots must never be treated as scratch. | accepted contract | LOC | Inspect current runner and DB-lifecycle source plus integration tests showing paired creation/cleanup, and demonstrate that measurement roots/databases survive scratch cleanup. Check current scoped deletion guidance; historical prose is not evidence. |
| LOC-04 | `local-state.md`: “Run outputs under data/” table, data/verification row | Verification-corpus output is disposable only because a complete gate run fully regenerates it. | accepted contract | LOC | Run the current gate from a clean state and compare expected fixture/golden outputs; add or confirm a test that removes the output first and proves full regeneration without relying on undeclared local state. |
| LOC-05 | `local-state.md`: “Run outputs under data/” table, data/jarvis-* row, first two retention statements | Regenerable Jarvis fixtures may be rebuilt, but provenance must remain attached to the measurement outputs it describes. | accepted contract | LOC | Check current preparation code and a clean regeneration test for fixtures, then verify report/provenance validation rejects or detects measurement outputs whose required provenance is absent. |
| LOC-06 | `local-state.md`: “Run outputs under data/” table, data/jarvis-* row, final sentence | The named shared-cache path for pinned Jarvis clones is discarded as volatile topology, not retained as a deletion contract. | intentionally discarded | NONE | Confirm no accepted contract depends on this exact path or cache-sharing arrangement; any current cache warning should be derived from implementation and placed in scoped operator guidance. |
| LOC-07 | `local-state.md`: “Run outputs under data/” table, loose data/github_com_* directories row | The ownerless paper-milestone retention note for loose pre-profile run directories is discarded. | intentionally discarded | NONE | Before any actual deletion, identify whether current registered reports reference the artifacts and apply an explicit archival policy. Do not infer retention from the retired note. |
| LOC-08 | `local-state.md`: “Other local state” table, database/teralizer row | Live PostgreSQL data-directory contents are managed storage and must not be directly edited or committed. | accepted contract | DBL | Verify the current deployment’s actual data-volume mapping, ignore rules, and backup/restore procedure; add or confirm checks preventing tracked PGDATA files and scoped guidance warning against direct mutation. |
| LOC-09 | `local-state.md`: “Other local state” table, database/db.sqlite and data-dev.zip rows | The ownerless paper-milestone retention notes for the legacy SQLite file and root data archive are discarded. | intentionally discarded | NONE | Determine from current report manifests and provenance records whether either artifact is referenced before archiving or deleting it; the historical keep-until-paper language supplies no evidence. |
| LOC-10 | `local-state.md`: “Other local state” table, logs/ row | Pipeline logs are deletable only when they are not the sole registered evidence, active-run diagnostics, or retained-measurement provenance. | accepted contract | LOC | Audit current log consumers, report manifests, and active-run behavior; test clean reruns without old logs and document any logs that must be promoted to retained evidence before cleanup. |
| LOC-11 | `local-state.md`: “Other local state” table, project-configs/timeout-retry*.conf row | The timeout-retry configuration row and neighboring-guidance pointer are discarded as a volatile source/owner map. | intentionally discarded | NONE | Confirm current scoped guidance and generator source own any still-valid regeneration rule; do not migrate the retired path, glob, or cross-reference merely because it appeared here. |
| LOC-12 | `local-state.md`: “Other local state” table, fixture residue under verification/fixtures row | Generated fixture residue is disposable because the verification driver reconstructs or sweeps it; checked-in inputs and goldens are excluded from that deletion class. | accepted contract | LOC | Use a clean-fixture integration test to prove the listed classes of residue are recreated or unnecessary, verify the sweep cannot match source fixtures/goldens, and check current ignore rules. |
| LOC-13 | `local-state.md`: “Other local state” table, root transient junk row | Repository-root JVM crash logs and jqwik transient databases are disposable once no live diagnostic or process requires them. | accepted contract | LOC | Confirm current tools create only transient state matching the cleanup patterns, ensure cleanup excludes committed seeds or registered failure evidence, and test that subsequent runs do not require the files. |
| LOC-14 | `local-state.md`: “Other local state” table, scratch databases row | Runner-owned scratch databases may be lifecycle-created and dropped, but retained measurement databases must be protected from every scratch cleanup path. | accepted contract | DBL | Inspect lifecycle source and tests for allowlist/denylist behavior; run destructive-operation tests proving protected measurement databases cannot be selected by scratch cleanup, and verify the current protection registry is authoritative rather than relying on the historical names. |
| LOC-15 | `local-state.md`: “Other local state” table, data/detached/<name>.{pid,meta,log} row | Detached-run tracking files may be deleted only after the process stops; lifecycle operations reap processes, while tracking files are not themselves process control. | accepted contract | LOC | Test launch, status, stop, and sweep against live and stale records; prove cleanup does not orphan a running process or signal a reused PID, and confirm no retained measurement provenance depends on the tracking log. |
| RQ6-01 | `rq6-analysis.md`: “Canonical RQ6 analysis” and “Publishing to a consuming repository” — canonical implementation and “Never hand-copy” rules | Every publishable RQ6 figure, table, and numeric prose citation must come from the registered canonical report, not ad hoc queries or copied outputs. | accepted contract | EXA | Confirm the current registry identifies one authoritative RQ6 report, trace every exported table/figure/prose macro to a registered output, and check source plus integration tests reject or bypass no required provenance step. |
| RQ6-02 | `rq6-analysis.md`: “Publishing to a consuming repository” — all-or-nothing publication and stale-output rationale | Publication atomically rebuilds all registered reports and refuses single-report publication to prevent mixed fresh and stale shared outputs. | executable checked fact | REX | Run publication tests from mixed fresh/stale fixtures and verify the runner rebuilds all registered reports, rejects a non-all report selection, and publishes one internally consistent output set. |
| RQ6-03 | `rq6-analysis.md`: “Publishing to a consuming repository” — per-report corpus agreement and scratch-corpus restriction | Each report declares and validates its own required corpus identity; scratch-corpus override is incompatible with all-report publication. | executable checked fact | RIN | Inspect current registry schema and runner source, then execute fixtures with matching, mismatching, and scratch corpus identities to verify per-report validation and the all-report override rejection. |
| RQ6-04 | `rq6-analysis.md`: “Authoritative definitions” — centralized definitions and prohibition on duplicated SQL | One executable mapping centralizes all RQ6 mechanism and denominator semantics. | executable checked fact | MEE | `_exclusion_evidence.py` owns the mechanism registry, typed relations, and reader collapse. `_generalization_funnel.py` owns named funnel populations. Focused tests reject drift, unknown evidence, and incompatible totals. |
| RQ6-05 | `rq6-analysis.md`: “Authoritative definitions” — ineligible project boundary and treatment of later failures | Only failures before the registered pre-analysis project boundary make a project ineligible; later failures remain outcomes within the eligible RQ6 funnel. | accepted contract | EXA | Create lifecycle fixtures with failures immediately before and after the eligibility boundary and verify report tests classify only the former as ineligible while retaining the latter in the eligible denominator. |
| RQ6-06 | `rq6-analysis.md`: “RQ6 result hierarchy” item 1 — project funnel, headline applicability, and pre-reduction intermediate measure | The project funnel starts with eligible projects, reports lifecycle survivors, computes final-usable applicability over eligible projects, and separately reports validated generalization before reduction. | accepted contract | EXA | Use a hand-checkable fixture spanning all lifecycle outcomes to recompute the eligible denominator, final numerator, intermediate validated-generalization count, and survivor sequence; compare them with the current golden output. |
| RQ6-07 | `rq6-analysis.md`: “RQ6 result hierarchy” item 2 — eligible-corpus item outcomes and generalization lifecycle distinction | Item outcomes cover tests, assertions, and generalizations only in eligible projects, and generated-filter-passed inclusion is distinct from final post-reduction usability. | accepted contract | EXA | Build fixtures containing ineligible projects and generalizations that pass generation filtering but fail later; verify exclusion of ineligible items and the distinct generated-filter-passed versus final-usable measures in report tests. |
| RQ6-08 | `rq6-analysis.md`: “RQ6 result hierarchy” item 2 — warning that display buckets did not represent all exclusion mechanisms | RQ6 reporting must totally classify every exclusion mechanism under the accepted taxonomy with explicit filtering semantics and lifecycle authority. | accepted contract | EXA | Enumerate the current mechanism registry, prove via schema constraints or exhaustive tests that each mechanism has exactly one authoritative classification and lifecycle point, and verify report labels match those semantics. |
| RQ6-09 | `rq6-analysis.md`: “RQ6 result hierarchy” item 2 — historical two-bucket defect description | The retired report described a two-bucket mapping that classified generation gates and javac quarantine as filtering and inline capability exclusions as failures. | stale/disproved | EXA | Current rq6_causes.py separates filtered, refused, unsupported, and failed outcomes and restricts filter rejection to filter-class identities; focused RQ6 tests pass. |
| RQ6-10 | `rq6-analysis.md`: “RQ6 result hierarchy” item 3 — filter-decision scope, decision classes, and overlap | Filter-decision reporting is limited to eligible projects, counts distinct evaluated entities per filter and decision, and preserves overlapping populations rather than summing them as unique exclusions. | accepted contract | EXA | Use fixtures where one entity is evaluated by multiple filters and has representative decisions; independently derive per-filter distinct counts and verify the report preserves overlap without presenting their sum as a unique-exclusion total. |
| RQ6-11 | `rq6-analysis.md`: “Supporting analysis for prose” — supporting cause, telemetry, confidence, sensitivity, and lifecycle metrics | Current RQ6 diagnostics qualify primary measures but do not replace their registered project or entity denominators. | executable checked fact | RQ6 | rq6_causes.py builds diagnostic tables separately from _funnel and breakdown populations; test_rq6_causes.py verifies the registered output set and denominator-bearing metrics. |
| RQ6-12 | `rq6-analysis.md`: “Supporting analysis for prose” — raw stage counts and raw entity totals | Raw stage outcomes and raw entity totals are operational diagnostics, not the eligible-project funnel or eligible-corpus item denominators. | accepted contract | EXA | Construct a fixture with ineligible projects and repeated operational attempts, compare raw operational/entity totals with report populations, and verify tests keep both raw measures out of the authoritative funnel and item denominators. |
| RQ6-13 | `rq6-analysis.md`: “Supporting analysis for prose” — measure-and-denominator citation requirement | Every quoted RQ6 figure must identify both its measure and denominator. | accepted contract | EXA | Check generated table captions, notes, prose macros, and metadata against the report registry; add a lint or schema test that fails any publishable measure lacking an explicit denominator definition. |
| RQ6-14 | `rq6-analysis.md`: “Supporting analysis for prose” — cross-database comparison rule | Cross-database comparisons run the same registered report code for each database and align projects by stable corpus path identity, never local numeric IDs. | accepted contract | EXA | Run two fixtures with identical projects but permuted database IDs and verify identical comparisons through stable path joins; separately verify that differing report-code versions are rejected or explicitly versioned. |
| RQ6-15 | `rq6-analysis.md`: “Freezing a corpus result” — complete-corpus consistency gate | Final RQ6 generation is refused unless project files, completion markers, attempt ledger, and database paths jointly form one complete consistent corpus. | executable checked fact | RIN | Exercise complete and independently corrupted fixtures for each required corpus component; verify generation succeeds only for the complete consistent case and fails loudly for missing, extra, or mismatched records. |
| RQ6-16 | `rq6-analysis.md`: “Freezing a corpus result” — snapshot and transaction requirements | All queries for one final RQ6 materialization run in one read-only repeatable-read database snapshot. | executable checked fact | RIN | Inspect transaction setup and run a concurrency test that mutates relevant rows during generation; verify every output observes one snapshot and that attempted writes from the report transaction fail. |
| RQ6-17 | `rq6-analysis.md`: “Freezing a corpus result” — provisional intermediate runs and final generated evidence | Intermediate corpus outputs are explicitly provisional; only completeness-checked final generated artifacts are citable. | accepted contract | EXA | Verify current artifact manifests distinguish provisional from final runs, publication rejects provisional artifacts, and a complete final run records the completeness and snapshot checks required for citation. |
| RQ6-18 | `rq6-analysis.md`: Command blocks under “Canonical RQ6 analysis,” “Publishing to a consuming repository,” and “Freezing a corpus result” | Historical shell commands, environment examples, and duplicate run/publish invocations are discarded as operational snippets. | intentionally discarded | NONE | Confirm supported invocation syntax and options from current CLI source/help tests; ensure no durable behavior is documented solely by the retired snippets before removing them from migration scope. |
| RQ6-19 | `rq6-analysis.md`: “Canonical RQ6 analysis” default database and “Freezing a corpus result” default corpus/configuration paths | The historical v6 database name and version-specific default data/configuration directories are discarded. | intentionally discarded | NONE | Check current defaults from executable configuration tests and registry data, and verify durable corpus-identity/completeness rules are covered independently of these retired names. |
| RQ6-20 | `rq6-analysis.md`: “Canonical RQ6 analysis” output locations and “Publishing to a consuming repository” consumer directory descriptions | Historical output directories, tracking statuses, and consumer-subdirectory inventories are discarded while artifact authority, atomic publication, and provenance remain. | intentionally discarded | NONE | Inspect current artifact manifests and publication tests to identify actual destinations, while confirming that authority, atomicity, and stale-output prevention are tested without relying on retired path inventories. |
| RQ6-21 | `rq6-analysis.md`: Bullet list under “Authoritative definitions” | The historical source-file ownership map for report modules is discarded; the live registry and source/tests define ownership. | intentionally discarded | NONE | Resolve current implementations through the live registry and dependency graph, then confirm the centralized-semantics claim has source and test coverage before dropping the historical file list. |
| RQ6-22 | `rq6-analysis.md`: “RQ6 result hierarchy” numbered shapes, stage references, table labels, and EM-1 through EM-8 reference | Historical stage numbers, table labels, defect IDs, and presentation shapes are discarded; only eligibility, lifecycle, denominator, overlap, and classification semantics remain. | intentionally discarded | NONE | Map every durable semantic represented by the old labels to a current contract entry and fixture/golden assertion, then verify no current consumer depends on the retired identifiers as semantic authority. |

## Retained RQ6 consumer review

The downstream claim inventory keeps four result groups. `RETAINED_METRIC_KEYS` and `RETAINED_TABLE_KEYS` in `rq6_causes.py` own the executable identities.

| Candidate group | Decision | Current evidence owner |
|---|---|---|
| Eligible-project funnel and end-to-end applicability | retained | `tab-processing-failures` and its project metrics |
| Attempted, emitted, filter-adjudicated, filter-passed, validated, reduced, and final-usable generalizations | corrected | `rq6_generalization_funnel` and its shared population metrics |
| Test, assertion, and generalization mechanisms plus the reader collapse | corrected | `rq6_exclusion_mechanisms`, `tab-exclusions-breakdown-extended`, and `tab-exclusions-filtering-extended` |
| Persisted widening-refusal causes and bounded JPF and MUT diagnostics | retained | `rq6_widening_refusals`, `rq6_jpf_exception_causes`, and `rq6_mut_choice_sensitivity` |
| Historical four-bucket call-shape taxonomy | removed | No executable classifier or reproducible input exists. The retained-consumer test requires no replacement output. |
| Historical database names, row positions, stage labels, copied values, and ad hoc query results | removed | The registered run manifest and semantic keys replace these volatile identities. |

Each rate metric names a numerator and denominator key. Each operand names its entity level and report input role. The run manifest owns corpus identity and source revision.

## Conditional causal-audit decision

The thesis retains two qualitative causal explanations: EXC-18 and EXC-20. Existing executable fixtures close both explanations on the pinned JPF/SPF stack:

- `NullConcreteRefusalShapeTest` and `OutputSpecClassifierTest` distinguish computed booleans, local stores, concrete returns, field and array reads, callee returns, and loop accumulation.
- `DivergenceRiskCaptureTest` and `TestGeneralizationListenerConcretizationTest` distinguish native-boundary concretization and later divergence risk.
- `WideningLicenseTest` binds those observations to the current license verdict and stable refusal code.

The focused fixture run passed on 2026-08-22. No retained causal explanation requires reviewer interpretation. Therefore, this change creates no audit sample, schema, report input, artifact, or placeholder registration. The identity-free historical source audit remains discarded under EXC-21.
